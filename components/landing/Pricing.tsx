export function Pricing() { return <section className="section" id="pricing"><div className="container"><p className="eyebrow">Pricing</p><h2>Start lean, scale with shipment volume.</h2><div className="grid" style={{gridTemplateColumns:'repeat(auto-fit, minmax(240px, 1fr))'}}>{[['Starter','$299','For one warehouse team'],['Operations','$799','For multi-site claim control'],['Enterprise','Custom','For connected vendor networks']].map(([name, price, copy]) => <div className="card" key={name}><h3>{name}</h3><h2>{price}</h2><p className="metric">{copy}</p></div>)}</div></div></section>; }
