import { recentShipments } from '@/lib/constants';
export function Demo() { return <section className="section" style={{background:'white'}}><div className="container"><p className="eyebrow">Demo</p><h2>From exception to evidence packet in minutes.</h2><div className="card" style={{overflowX:'auto'}}><table className="table"><thead><tr><th>Shipment</th><th>Vendor</th><th>Status</th><th>Risk</th></tr></thead><tbody>{recentShipments.map((s) => <tr key={s.id}><td>{s.id}</td><td>{s.vendor}</td><td>{s.status}</td><td><span className={'status ' + (s.risk === 'High' ? 'red' : s.risk === 'Medium' ? 'amber' : 'green')}>{s.risk}</span></td></tr>)}</tbody></table></div></div></section>; }
