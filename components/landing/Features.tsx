export function Features() { const features = ['AI claim readiness scoring','Shipment and invoice matching','Warehouse exception timeline','Vendor performance intelligence','Role-based operations workspace','Exportable audit reports']; return <section className="section"><div className="container"><p className="eyebrow">Features</p><h2>One operating layer for disputes, shipments, and proof.</h2><div className="grid" style={{gridTemplateColumns:'repeat(auto-fit, minmax(250px, 1fr))'}}>{features.map((feature) => <div className="card" key={feature}><h3>{feature}</h3><p className="metric">Built for logistics teams that need fast answers and defensible records.</p></div>)}</div></div></section>; }
