export function Charts() { return <div className="card"><h3>Dispute trend</h3><p className="metric">Open claims and recoverable value over the last six periods.</p><div className="chart-bars">{[42,68,52,78,61,88].map((height, index) => <span key={index} style={{height:height + '%'}} />)}</div></div>; }
